'use strict';

const initialState = {
  cheeses: []
};

const cheeses = (state=initialState, action) => {
  if (aciton.type === action.______ ){
    return Object.assign({}, state, {cheeses: })
  }
  else {
    return state 
  }
}